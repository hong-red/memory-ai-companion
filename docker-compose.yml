version: '3.8'

services:
  memory-ai-companion:
    build: .
    container_name: memory-ai-companion
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - JWT_SECRET=${JWT_SECRET:-your-super-secret-jwt-key-change-this-in-production}
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./database.sqlite:/app/database.sqlite
    networks:
      - memory-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # 可选：使用 Nginx 反向代理
  nginx:
    image: nginx:alpine
    container_name: memory-ai-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - memory-ai-companion
    networks:
      - memory-network

networks:
  memory-network:
    driver: bridge

volumes:
  data:
  uploads:
